bootstrap.servers = localhost:9092
zookeeper.connect = localhost:2181

kafka.group.id = yauza
kafka.topic.input = yauza-input
kafka.partition = 3


#spark.master = spark://localhost:7077
spark.master = local

spark.streaming.receiver.maxRate = 100000
spark.streaming.kafka.maxRatePerPartition = 100000

benchmark.hdfs = hdfs://localhost:9000
benchmark.datafile = /yauza-benchmark/datafile.json

benchmark.messages.number = 1000000
benchmark.messages.per.second = 0
benchmark.datagenerator.directuploader.threads = 1

benchmark.data.uniqueusers.number = 1000
benchmark.data.purchase.maxprice = 1000
benchmark.data.purchase.percentage = 10

benchmark.runtime.partitions.number = 3
benchmark.runtime.window.duration = 10

benchmark.runtime.duration = 60
