bootstrap.servers = {{bootstrap.servers}}
zookeeper.connect = {{zookeeper.connect}}

kafka.group.id = {{kafka.group.id}}
kafka.topic.input = {{kafka.topic.input}}
kafka.partition = {{kafka.partition}}

spark.master = {{spark.master}}

spark.streaming.receiver.maxRate = {{spark.streaming.receiver.maxRate}}
spark.streaming.kafka.maxRatePerPartition = {{spark.streaming.kafka.maxRatePerPartition}}

benchmark.hdfs = {{benchmark.hdfs}}
benchmark.datafile = {{benchmark.datafile}}

benchmark.messages.number = {{benchmark.messages.number}}
benchmark.messages.per.second = {{benchmark.messages.per.second}}
benchmark.datagenerator.directuploader.threads = {{benchmark.datagenerator.directuploader.threads}}

benchmark.data.uniqueusers.number = {{benchmark.data.uniqueusers.number}}
benchmark.data.purchase.maxprice = {{benchmark.data.purchase.maxprice}}
benchmark.data.purchase.percentage = {{benchmark.data.purchase.percentage}}

benchmark.runtime.partitions.number = {{benchmark.runtime.partitions.number}}
benchmark.runtime.window.duration = {{benchmark.runtime.window.duration}}

benchmark.runtime.duration = {{benchmark.runtime.duration}}
